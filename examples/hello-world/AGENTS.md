Before changing greeting responsibilities or formatting, run `npm run ag -- context --file src/formatter.js` (or the affected file) and read the returned declarations. Maintain `architecture/graph.json` alongside implementation, then run `npm test`, `npm run ag -- validate`, `npm run ag -- generate`, and `npm run ag -- check`. Generated guidance is derived. A structural pass is not implementation conformance.
