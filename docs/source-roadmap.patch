diff --git a/docs/architecture-graph/architecture-graph-roadmap.md b/docs/architecture-graph/architecture-graph-roadmap.md
index f6433f70..79c42bc5 100644
--- a/docs/architecture-graph/architecture-graph-roadmap.md
+++ b/docs/architecture-graph/architecture-graph-roadmap.md
@@ -46,7 +46,7 @@ become canonical graph entities merely because they contributed to its history.
 
 ## Proposed
 
-- AG-Y — Canonical Graph Semantic Proof Authority (Gate 0 complete; bounded implementation authorized; Gate A planned). Adds a production-loader-derived semantic inventory of registered graph objects and relationships so the rooted proof system can establish exact graph-object sets, ownership, endpoint integrity, and relationship closure without treating YAML files or serialized text as the architecture. AG-Y is a generic prerequisite for the blocked Slice 7M-2 canonical AAG self-model; it does not implement that self-model or its projection subject protocol.
+- AG-Y — Canonical Graph Semantic Proof Authority (Gate A accepted; Gate B implementation complete; acceptance is determined only by final source-frozen evidence). Adds a production-loader-derived semantic inventory of registered graph objects and relationships, then canonically self-applies the existing rooted proof, maintenance, admission, scheduling, and read-only projection chain to the sole loader, sole inventory, exact implementation closure, exact ownership/authority relationships, typed relationship closure, and negative space. AG-Y is a generic prerequisite for the blocked Slice 7M-2 canonical AAG self-model; Gate B does not implement that self-model or the Slice 7M-3 projection-subject/handoff protocol.
 - AG-R — Per-Module Canonical YAML Split (alpha-engine graph only). Phase 0 decision gate complete: **NO-GO recorded 2026-07-11** — 32 cross-module `owns`/`supersedes` blockers dispositioned (see the spec's Phase 0 Decision and `write-ag-r-split-blocker-disposition-audit.ts`). Phase 1 migration waits on a bridge-ownership classification slice (and optionally a reporting/runtime-viewport boundary re-draw), then a re-run of the gate.
 
 ## Sequencing rationale
